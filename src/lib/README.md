# src/lib/

Shared utility helpers.

## Files
- `utils.ts`: Exports `cn(...)` class merge helper (`clsx` + `tailwind-merge`), used by most UI components.
- `supabase.ts`: Supabase client initialization + environment/config checks.
