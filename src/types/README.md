# src/types/

Shared TypeScript interfaces/constants used across features.

## Files
- `styleTemplates.ts`: Defines `StyleTemplate`, default style templates, and global system prompt text used during generation.
