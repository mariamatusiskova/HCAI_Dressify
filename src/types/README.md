# src/types/

Shared TypeScript interfaces/constants used across features.

## Files
- `styleTemplates.ts`: defines the `StyleTemplate` shape, default style template presets, and the global fallback system prompt used during generation.
