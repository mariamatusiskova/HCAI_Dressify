// very basic test file to confirm your test setup works
// - to make sure Vitest is installed correctly
// - to confirm the test runner can find and run test

import { describe, it, expect } from "vitest";

describe("example", () => {
  it("should pass", () => {
    expect(true).toBe(true);
  });
});
